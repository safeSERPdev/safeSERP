# Backend

SafeSERP v1 verifies sites from a bundled JSON registry inside the browser extension. No server logic is required yet.

Future options (not implemented): hosted registry updates, phishing reports.
